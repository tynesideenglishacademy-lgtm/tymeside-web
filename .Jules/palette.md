## 2024-09-07 - Language Toggle Accessibility and UX Enhancement
**Learning:** Icon-only buttons, especially in global navigation contexts like language switchers, must be discoverable for sighted users via `title` attributes and screen reader accessible via localized `aria-label`s to ensure all users understand their function.
**Action:** Always add localized `title` and `aria-label` to icon-only buttons in the navigation.
